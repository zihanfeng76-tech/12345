import { ProcessingSettings } from './types';

export const DEFAULT_SETTINGS: ProcessingSettings = {
  colorCount: 6,
  ignoreGrayscale: true,
  samplePrecision: 5,
  brighten: false
};

export const MOCK_PALETTE_NAME = "Dunhuang Impression";